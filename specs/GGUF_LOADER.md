# GGUF LOADER (M10 v1)
Header textual GGUF + version(2d) + nTensors(3d) parseados (magicOk/version/
nTensors em GgufInfoRec). Metadados/vocabulário/weights binários: PENDENTE-
FLT/SIMD (Q2) — parser binário completo entra com Runtime FP.
