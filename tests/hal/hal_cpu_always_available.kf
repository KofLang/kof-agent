test "cpu always available" {
    assert(halDetect().size >= 1)
    assert(halSelect("").kind == "CPU")
}

test "cpu always available" {
    assert(halDetect().size >= 1)
    assert(halSelect("").kind == "CPU")
}
main() {
}
