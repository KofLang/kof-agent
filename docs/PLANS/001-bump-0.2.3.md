# PLANO 001 — Bump do baseline do compilador: cb4aede → 0.2.3-beta (25d017a)

**Data:** 2026-08-29 · **Status:** em execução · **Owner:** sessão atual

## Contexto

O agente está pinado em `cb4aede` (v0.1.1-alpha). O upstream Kof4j avançou 48
commits desde então até o HEAD `25d017a` (0.2.3-beta). O
`PENDING_COMPILER_UPDATE.txt` apontava `7afdbb5` (AES-GCM nativo), mas o HEAD
atual já o contém e traz mais 76 commits além dele (GC mark-sweep nativo,
fixes de string/List/subclass no native, scheduler, KofC, KofScript).

Além do bump em si, há uma incoerência estrutural: os scripts apontam para
`/home/luna/kof/Kof4j` (máquina anterior) — o checkout real agora é
`/home/mel/Documentos/Kof4j`.

## Objetivos

1. Bumpar o baseline do agente para o HEAD 0.2.3-beta e fixar o caminho do
   checkout de forma portátil.
2. Re-sweep completo do ledger de bugs contra o novo HEAD (repros mínimos +
   retestes dirigidos).
3. Rodar todas as suítes nativas do agente e medir o delta vs 0.1.1-alpha.
4. Remover workarounds de bugs confirmados como fechados (regra D0015:
   bug fechado upstream = simplificação no agente).
5. Sincronizar toda a documentação de compatibilidade (status.md,
   compiler-bugs.md, ledger.md, compatibility-matrix.md, upstream-status.md,
   PENDING_COMPILER_UPDATE.txt) e commitar por etapa.

## Foras de escopo (próximos planos)

- Treino QLoRA do KofLM (PLANO 002 — venv + torch + execução).
- Continuação M31 (kernels completos no GraphExecutor).
- Relatório upstream formal (após o sweep, com evidências atualizadas).
- GPU/I RADV (RX 6600).

## Passos

| # | Etapa | Critério de done |
|---|-------|------------------|
| 1 | Caminho portátil do kof: scripts usam `KOF4J_ROOT`/`KOF` resolvidos; remover hardcode `/home/luna/...` (scripts/*.sh, training/RUN_OVERNIGHT.sh, scripts/build_koflm_dataset.py) | `git grep -l luna scripts/ training/` vazio; scripts rodam sem KOF no PATH |
| 2 | Build limpo do Kof4j no HEAD + `lib/kof.jar` atualizado | `mvn clean package -DskipTests`; `bin/kof version` = 0.2.3-beta; hello world nativo+jvm |
| 3 | Sweep de regressões: N1 N3 N4 N6 N7 N8 N9 N10 N11 N12 N13 N16 N17 N18 N19 J4 (+ SEM025/lastIndexOf) | Tabela de resultados com exit codes reais em `docs/compiler/reports/sweep-25d017a.md` |
| 4 | Suítes do agente: test.sh (núcleo+JVM+stress), ws, tools, corpus, brain, planner, exec, ai, hal, nn, f3, m16, m17, m18, rc1, f4/f5/f6 | Contagem por suíte registrada; delta vs baseline documentado |
| 5 | Repros N18/N19 atualizados: reescrever como TU autossuficientes (independentes das PARTs do agente) para o ledger | `kof test regressions/N18*/repro*.kf --target native` e `regressions/N19*/unit_engine.kf` reproduzem (ou não) sem build.sh |
| 6 | Remoção de workarounds (apenas bugs CONFIRMADOS fechados neste sweep): `00_core.kf` (splitStr/parseIntStr p/ N2/N4), `check_compat.sh` (regras de N6/N7/N9), `.kofargs` p/ N3 se `main(String[])` funcionar nativo | `scripts/check_compat.sh` passa; suítes núcleo ainda verdes |
| 7 | Sincronização de docs: status.md (snapshot+fila), docs/compiler-bugs.md (nova seção do sweep), docs/compiler/ledger.md, bug-ledger.md, compatibility-matrix.md, upstream-status.md, PENDING_COMPILER_UPDATE.txt, README.md (linha do compilador) | Sem menção a baseline obsoleto sem nota de data |
| 8 | Commits por etapa (script-path, sweep+docs, workarounds) | `git log` limpo; `git status` sem sujeira |

## Riscos

- **N10-progressivo:** suítes grandes (f3) podem seguir SIGSEGV — esperado;
  registrar, não mascarar.
- **Regressões do 0.2.3:** GC novo no native (90b8164/ca3dacf) pode mudar
  comportamento de artefatos grandes — o sweep existe exatamente para isso.
- **Workaround removem-se cedo demais:** só remover com evidência de exit
  code no sweep; caso contrário, manter e marcar "retestar".

## Entrega

Repo com baseline 0.2.3-beta, caminho portátil, ledger atualizado com
evidências reais, workarounds fechados removidos e docs consistentes.
Próximo passo natural: PLANO 002 (treino QLoRA).
