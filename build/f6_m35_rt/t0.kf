test "koflm load and warmup and chat" {
    var h = openGGUF("fixtures/tiny.gguf")
    var k = KoflmRuntime()
    k.registerOfficialModel()
    k.loadModel(h)
    k.warmup()
    var s = k.chat(1, 4)
    var n = s.length
    assert(n > 0)
}