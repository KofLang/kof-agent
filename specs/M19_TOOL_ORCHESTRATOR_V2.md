# M19_TOOL_ORCHESTRATOR_V2

Tool Orchestrator v2 — Scheduler (fila por prioridade/categoria), Timeouts por tool, Retry Policy configurável (backoff exponencial), Parallel execution (scheduler cooperativo M1), Cost Budget (orçamento por sessão), Permission Audit log, Tool Tracing (spans), Tool Metrics agregadas.

## Definition of Done

- Spec revisada.
- Implementação em Kof compilando para Native.
- Testes one-per-process.
- Benchmark baseline.
- Compatibility sweep.
