test "cpu always available" {
    assert(halDetect().size >= 1)
    assert(halSelect("").kind == "CPU")
}

test "override selects backend when present" {
    assert(halSelect("CPU").name == "cpu-simd")
}
main() {
}
