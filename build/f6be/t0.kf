test "cpu backend init and matmul" {
    var b = CPUBackend()
    b.init()
    assert(b.ready)
    var x = tensorNew(2, 2)
    tensorSet(x, 0, 0, 3)
    var m = b.matmul(x, x)
    assert(tensorGet(m, 0, 0) == 9)
}